name: First yaml file
on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world srajan saxena"

      - name: Multiline Command
        run: |
          node -v
          npm -v
